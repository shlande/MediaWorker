# Review-Remediation Summary (T1–T23)

One line per todo. Evidence path → conclusion. Generated by T23 repo-wide
verification gate. Full gate output: `.omo/evidence/task-23-review-remediation.log`.

Verdict legend:
- PASS — todo complete, evidence archived, gates green at T23.
- PASS(watch) — PASS but a note for downstream (e.g. flaky test, doc-only).

T1: .omo/evidence/task-1-review-remediation.log — PASS (metadata package migrated to internal/storage/metadata; grep zero residual; `go build ./...` green)
T2: .omo/evidence/task-2-review-remediation.log — PASS (MigrateAll exported; NewPGMetadataClient auto-runs migration; idempotency verified)
T3: .omo/evidence/task-3-review-remediation.log — PASS (pipeline.go literal 2 removed; K=3 + K=0/neg→2 normalization unit tests green)
T4: .omo/evidence/task-4-review-remediation.log — PASS (ProcessResult.WorkDir + pipeline unified cleanup + startup sweep verified)
T5: .omo/evidence/task-5-review-remediation.log — PASS (max_upload_bytes wired; ≤0 normalized to 10GiB; startup free-space Warn only)
T6: .omo/evidence/task-6-review-remediation.log — PASS (JWT CP-side policy: declared∩default matrix; expired/forged rejected)
T7: .omo/evidence/task-7-review-remediation.log — PASS (node-side declared_capabilities + refresh loop; server 500 keeps retrying)
T8: .omo/evidence/task-8-review-remediation.log — PASS (SyncPublisher libp2p event path; fail-closed startup check; publishFailures counter)
T9: .omo/evidence/task-9-review-remediation.log — PASS (GET /v1/blob-locations/{hash} + Edge-capability JWT auth; 401/403/404 branches)
T10: .omo/evidence/task-10-review-remediation.log — PASS (HTTPLocationClient + JWTClient.CurrentJWT; 404→empty, 500/401→error)
T11: .omo/evidence/task-11-review-remediation.log — PASS (EdgeRouter mux wired; 3-host proxy-to-master byte-equal; offline→fallback Warn)
T12: .omo/evidence/task-12-review-remediation.log — PASS (backhaulICPFetcher: ring.Get + icp.FetchFromPeer; 3-host fetch + cache write)
T13: .omo/evidence/task-13-review-remediation.log — PASS (migration 014 + two-phase soft-delete; mark/idempotent/rescue/circuit-break paths green)
T14: .omo/evidence/task-14-review-remediation.log — PASS (cmd/janitor once/interval + dry-run default; invalid interval + unreachable PG error out)
T15: .omo/evidence/task-15-review-remediation.log — PASS (config wiring batch A — edge-side dead fields alive; TestDHT_AdvertiseIntervalWired green)
T16: .omo/evidence/task-16-review-remediation.log — PASS (config wiring batch B — control-plane-side dead fields alive)
T17: .omo/evidence/task-17-review-remediation.log — PASS (config deletion batch; deprecated YAML keys emit Warn; docs reference updated)
T18: .omo/evidence/task-18-review-remediation.log — PASS (WarmCache PinChecker/PopSource wired; gossip subscribe loop running)
T19: .omo/evidence/task-19-review-remediation.log — PASS (gossip trust guard rejects stale/unknown peer popularity; trust model docs)
T20: .omo/evidence/task-20-review-remediation.log — PASS (/metrics on all three services; counters increment on key events; isolation fix archived in task-20-fix-isolation.log)
T21: .omo/evidence/task-21-review-remediation.log — PASS (docs target-state vs implemented-state annotations; Redis preserved as design asset)
T22: .omo/evidence/task-22-review-remediation.log — PASS (api/*.md refreshed; api/ dir tracked)
T23: .omo/evidence/task-23-review-remediation.log — PASS (repo-wide gate: build+vet+unit+integration+gofmt+config-consistency+go-mod-tidy all green; one config-consistency gap in configs/ingest-worker.yaml fixed during this gate)

## Final gate verdict (T23)

| Gate | Command | Result |
|------|---------|--------|
| (a)  | `go build ./... && go vet ./...` | PASS (exit 0 / 0) |
| (b)  | `go test ./... -count=1` | PASS (all 44 packages OK) |
| (c)  | `go test -tags=integration ./test/integration/... -count=1` | PASS |
| (d)  | `gofmt -l .` | PASS (zero output; 80 pre-existing files reformatted via `gofmt -w`) |
| (e)  | config consistency (5 Load* calls on `configs/*.yaml`) | PASS (after fixing T8 missing `control_plane` stanza in `configs/ingest-worker.yaml`) |
| (f)  | `go mod tidy` then `git diff go.mod go.sum` | PASS (zero diff — deps clean) |
| (g)  | 23-line summary file | PASS (this file, 23 T-lines above) |

## Issues found and fixed during this gate

1. **`configs/ingest-worker.yaml` missing `control_plane` stanza** — T8 added
   required fields `control_plane.multiaddr` and `control_plane.priv_key_path`
   to `LoadIngestWorkerConfig`, but the example YAML was not updated. Fixed by
   adding a documented example stanza. This is a config-consistency fix (the
   example must demonstrate the required field), NOT an assertion weakening.
2. **80 Go files needed `gofmt -w`** — pre-existing formatting drift across
   the repo (not introduced by any single T-todo). Fixed via `gofmt -w .` and
   verified `gofmt -l .` is now empty. Build + tests re-verified green after
   reformatting.

## Notes for downstream

- **No new dependencies introduced.** `go mod tidy` produced zero diff.
- **No assertion weakened.** All existing tests run as-is.
- **No integration test skipped.** Full `./test/integration/...` ran green.
- Pre-existing flakiness note (`TestIntegration_GossipSubPopularitySync`):
  did NOT manifest in this run — both unit and integration suites were green
  in a single shot.
